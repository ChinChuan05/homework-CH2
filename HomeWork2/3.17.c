#include <stdio.h>
#include <stdlib.h>

int account, begin, charged, total_credits, limit;

int Credit_Limit_Calculator() {
	printf("Enter account number: ");
	scanf("%d", &account);
	printf("Enter balance at the beginning of mouth");
	scanf("%d", &begin);
	printf("Enter total");
	scanf("%d", total_credits);
}
